/*
 * Base module for all other angular code. This acts as
 * general dependency / configuration code and allows
 * for easy / relatively safe namespacing.
 */
(function() {
    'use strict';
    var starredbucks = angular.module('starredbucks', ['ngCookies']);
})();

